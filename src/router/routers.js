/**
 * v0.0.1 去掉多余的文字管理路由
 */
import Layout from '@/layout'

/**
 * 自动注册路由，生成代码
 */

export default [
	{
		path: '/redirect',
		component: Layout,
		hidden: true,
		children: [
			// 自动生成路由代码位置
			// autoRouterStart
		

		]
	}
]
